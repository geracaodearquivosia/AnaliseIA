================================================================================
                DATA READER GENÉRICO - ROADMAP E DOCUMENTAÇÃO
================================================================================

📌 FLUXO ATUAL
1. Usuário faz upload de arquivo via interface Streamlit.
2. O app identifica a extensão (CSV, Excel, JSON).
3. Chamado o leitor específico (try_read_* no app.py ou _read_* no ingest.py).
4. DataFrame é limpo (remoção de colunas 'Unnamed', reset de índice).
5. Mostra-se preview (até 100 linhas) e info básica (linhas, colunas).
6. Disponibiliza botão de download em CSV padronizado.

================================================================================
⚠️ LIMITAÇÕES ATUAIS
- CSV:
  • Apenas encodings comuns ('utf-8', 'latin-1').
  • Apenas separadores padrão (vírgula, ponto e vírgula, tab, pipe).
- Excel:
  • No ingest.py carrega só a primeira planilha (não há escolha).
  • Arquivos muito grandes podem ser lentos.
- JSON:
  • Suporte a arrays de objetos e NDJSON simples.
  • JSONs muito aninhados podem gerar colunas difíceis de ler.
- Performance:
  • Todo o arquivo é carregado em memória (pandas). Para >50MB pode travar.
- UI:
  • Apenas preview e download em CSV. Não há análises ou gráficos ainda.

================================================================================
🚀 PRÓXIMOS PASSOS
1. GRÁFICOS GENÉRICOS
   • Implementar aba com seleção de colunas para gráficos (barras, linhas,
     pizza, histograma, scatter).
   • Usar Plotly para interatividade.

2. RESUMOS AUTOMÁTICOS
   • Criar aba com estatísticas básicas: contagem, média, mediana, valores nulos,
     top categorias etc.
   • Mostrar insights rápidos para qualquer dataset.

3. MELHORIAS NA LEITURA
   • Acrescentar mais encodings no CSV (ex: windows-1252).
   • Permitir escolha de planilha no ingest.py (igual ao app.py).
   • Adicionar suporte a Parquet (pyarrow).

4. PERFORMANCE
   • Implementar leitura em chunks para arquivos grandes.
   • Mostrar apenas amostras, com opção de carregar o dataset inteiro.

5. USABILIDADE
   • Permitir ao usuário renomear colunas na UI.
   • Melhorar mensagens de erro (ex: "separador incorreto" ao invés de falha genérica).

6. FUTURO (se der tempo)
   • Integração com IA (ex: Google Gemini) para gerar insights textuais.
   • Exportar relatórios automáticos (PDF/Excel).
   • Interface mobile-friendly.

================================================================================
✅ STATUS ATUAL
- Upload: FUNCIONANDO
- Leitura CSV: FUNCIONANDO (com encodings/sep comuns)
- Leitura Excel: FUNCIONANDO (UI escolhe planilha, ingest pega a primeira)
- Leitura JSON: FUNCIONANDO (array e NDJSON)
- Limpeza leve: FUNCIONANDO
- Preview + Download CSV: FUNCIONANDO

================================================================================
